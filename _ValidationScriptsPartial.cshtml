@* Шаблонная частичная страница со скриптами клиентской валидации. В проекте НЕ подключается (нигде нет @await Html.PartialAsync("_ValidationScriptsPartial")), поэтому проверка форм идёт на сервере (ModelState). *@
@* Библиотека jQuery Validation - проверка полей в браузере. *@
<script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
@* Адаптер, превращающий атрибуты data-val-* (их создают tag helpers из DataAnnotations) в правила jQuery Validation. *@
<script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>
